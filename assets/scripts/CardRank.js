// Card rank enum. Stored as integers in data files: Hard=1, Core=0, Easy=-1, Cold=2.
export const CardRank = Object.freeze({
    Hard: 'Hard',
    Core: 'Core',
    Easy: 'Easy',
    Cold: 'Cold',
});
